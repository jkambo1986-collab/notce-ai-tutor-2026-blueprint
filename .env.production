# Production build config (loaded automatically by `vite build`).
# The SPA is served same-origin by Django, so the API is reached via a
# relative path. Declared here (not via a shell env var) so the value is
# not mangled by MSYS/Git-Bash path conversion on Windows builds.
VITE_API_BASE_URL=/api
