from django.apps import AppConfig


# App configuration for the project's main `core` app, which holds the models,
# views, serializers, and services for the AI tutor and question bank.
class CoreConfig(AppConfig):
    name = 'core'
